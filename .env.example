# ── 資料庫 ────────────────────────────────────────────────────────────────
# 執行期查詢 —— Supavisor transaction mode（port 6543）。
# transaction pooler 不支援 prepared statements；程式碼已設 prepare:false（src/db/client.ts）。
# Supabase direct connection 是 IPv6-only 而 Vercel 不支援 IPv6，pooler 是必要條件。
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres"

# migration 專用 —— session mode（session pooler，port 5432）。drizzle-kit 用這條。
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres"

# ── 本機開發（docker compose up）──────────────────────────────────────────
# 用本機 Postgres 時，兩條都指向它即可：
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/scenephonie"
# DIRECT_URL="postgresql://postgres:postgres@localhost:5432/scenephonie"
