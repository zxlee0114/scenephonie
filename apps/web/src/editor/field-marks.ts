/**
 * 欄位裡那些一個字的記號。
 *
 * 住在自己的檔案，是因為**兩個不同的欄位都要用**（實體欄位與群演欄），而它們刻意不是同一個
 * 元件 —— 讓其中一邊去 import 另一邊的常數，會在兩個平行的欄位之間造出一條假的從屬關係。
 *
 * 記號本身是有意義的區別，不是裝飾：`📍`／`👤` ＝ 命中既有實體、`＋` ＝ 這一輪新建的、
 * `👥` ＝ 群演（**沒有跨場次身分**，見 CONTEXT.md 的「群演」詞條）、`✏️` ＝ **就地改手上
 * 那一筆**，而不是新建一筆：實體欄裡是改實體本身而不是這一場的叫法（票券 39），群演欄裡是
 * 改這一批而不是另外開一批（票券 40）、`↩︎` ＝ 原封不動放回去（什麼都沒改）。
 *
 * ⚠️ `👥` 是**身分**標記（「這東西是群演」），不是動作。所以「原封不動放回手上那一批」那一列
 * 用 `↩︎` 而不是 `👥`：同一份選單裡 `👥` 已經是「把這串字填回輸入框」的補字列（票券 40）。
 */
import type { EntityKind } from "./entity-field";

export const HIT_MARK: Record<EntityKind, string> = { location: "📍", character: "👤" };
export const NEW_MARK = "＋";
export const EXTRA_MARK = "👥";
export const RENAME_MARK = "✏️";
export const PUT_BACK_MARK = "↩︎";
/**
 * `↰` ＝ **退一階**（票券 48 的人數子選單第一列）。
 *
 * 它與 `↩︎` 刻意是兩個記號，即使在人數那一層按下去的結果看起來一樣：`↰` 說的是「這一階
 * 不改，回上一階」（名稱那一側的待定改動留著），`↩︎` 說的是「整輪編輯作廢」。導航語意
 * 不因為某一階段的結果重疊就合併（使用者裁決 2026-09-12）。
 */
export const BACK_MARK = "↰";

/**
 * `✕` ＝ **這一輪丟掉**（新增那一側的退出，票券 49 驗收回饋 2026-09-13）。
 *
 * 它與 `↩︎` 是兩件事，雖然都是「結束這一輪」：`↩︎` 有一批人要**原封放回原位**（編輯），
 * `✕` 手上什麼都沒有，丟掉的是打到一半的那串字。拿 `↩︎` 去印「放棄新增群演」會讓那一列
 * 承諾一個不存在的還原點。
 */
export const DISCARD_MARK = "✕";

/**
 * `✅` ＝ **確認這一輪的結果**（票券 48 第二輪，編劇裁決 2026-09-12）。
 *
 * 它從 `✏️` 手上接走「定案」那一列：`✏️` 從此只代表**還要繼續編輯**的動作（`修改數量…`），
 * `✅` 代表**這一下就結束**。原本兩者共用 `✏️`，於是同一份選單裡「再做一件事」與「做完了」
 * 長得一樣。
 */
export const CONFIRM_MARK = "✅";
/**
 * `💡` ＝ **唯讀提示**（票券 48 第二輪）。
 *
 * 抬頭那一行原本也印 `✏️`，與選項列的編輯記號撞在一起（使用者回報 2026-09-12）——
 * 而它根本不是一列選項（選不到、Enter 碰不到，票券 36 的分工）。記號跟著那條分工走：
 * 動作用動作的記號，說明用說明的記號。
 */
export const HINT_MARK = "💡";
