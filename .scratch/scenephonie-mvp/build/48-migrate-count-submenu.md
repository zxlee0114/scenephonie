# 48 — 遷移批次 4：改人數走一層子選單，預設值寫在看得見的地方

**What to build:** 編輯狀態的選單多一列 `修改數量…`，按下去進到**人數子選單**：
第一列是 `↰ 不修改數量（8），回上一步`，接著 `若干`、`1`、一個自由輸入格，格子底下一行
唯讀提示。挑完人數**退回描述那一關**，名稱還能接著改。

**Blocked by:** 47、37（⌘Z 把編劇打的字還回輸入框）

**Status:** in-review

## 版面

```
修改（原本是 8）
┌──────────────────────────────────┐
│ ↰ 不修改數量（8），回上一步        │
├──────────────────────────────────┤
│ 若干                             │
│ 1                                │
├──────────────────────────────────┤
│ ▌                                │
│ { 提示 ／ 預覽 ／ 警告 }          │
├──────────────────────────────────┤
│ ↩︎ 不修改，返回                   │
└──────────────────────────────────┘
```

原本提的 `10+／20+／30+／40+` 階梯**拿掉**（編劇：「好像可有可無」）—— 它是猜的，而自由
輸入格加即時預覽比猜四個數字誠實。`1` 那一列只在與第一列不重複時才印（原本就是 1 的話
不必印兩次）。

## `↰` 與 `↩︎` 是**兩列**，不合併

使用者裁決 2026-09-12：**不要因為兩者在這一層結果相同就合併成同一個動作，它們代表的是
導航語意。**

- `↰ 不修改數量（8），回上一步` —— 退回描述那一關，名稱那一側的待定改動**留著**。
  它本身不是兩件事的合體，它是一列 `↰`，只是順便說出它不動的那個值。
- `↩︎ 不修改，返回` —— **整輪編輯作廢**，待定人數與名稱改動一起丟，手上那一筆原封放回。

（票券 41 第八輪原文寫成「一列兼兩職」，那句話**已被推翻**，不要照字面只做一列。
票券 42 第 2 條「選單走到哪一階段它都要在」在這裡同樣適用，所以 `↩︎` 在子選單裡也在。）

## `8（原訂）` 那條裁決搬到了第一列

第七輪提的 `8（原訂）` 解決的是同一個坑：第二關的預設若是「若干」，修改時它會**偷偷抹掉
原本的 8**。把預設值**從行為裡搬到畫面上**，那個坑就不存在了。第八輪之後它的位置在
`不修改數量（8），回上一步` 這一列 —— 措辭用「原訂」是編劇原話，但這一列已經不需要那兩個字。

再進子選單時第一列跟的是**待定值**（挑過 2 之後是 `不修改數量（2），回上一步`），因為那是
「現在的樣子」。

## 那一行提示永遠有話說

它回答的始終是同一個問題 —— **「我現在按下去會得到什麼」**（票券 40 給第一列立的規矩）。
所以它**不會因為讀不出來就消失**，只是換一副語氣：

| 框裡 | 那一行印 |
|---|---|
| 空 | `人數（8）、區間（3~5、3-5、10+）` ← 格式說明 |
| `3~5` | `路人（3-5）` ← 預覽 |
| `3~` | `⚠️ 「3~」還讀不出來 —— 現在離開會記成「路人（8）」` ← 警告 |

文案在票券 44 的 `countHintText` 裡，這一批只負責把它接上並證明它跟著框裡的字變。
⚠️ 它與票券 36 那條「標籤放結果、說明另外放」的分工要對一次 —— 兩者都不是選項列，
應該沒有衝突，實作前確認並把結論寫進註解。

## 待定人數是編輯狀態的一部分

挑了 2、回到主狀態時，那一批**還沒變** —— 變的是「按下去會得到什麼」。於是票券 40 第一列
自己就說對了：只改人數、沒碰名稱時它印 `✏️ 把「路人（8）」改成「路人（2）」`（`after` 用
待定值算）。**不必為這條路另外寫措辭。**

抬頭印的仍是**原值**（`✏️ 正在編輯「路人（8）」…`）—— 它說的是「我在編輯誰」，不是
「我打算改成什麼」，那是第一列的事（票券 36 的分工）。⚠️ 這一段的後半被下面那節的
編劇裁決改寫了：抬頭的**人數那一段**要跟著待定值走（挑過 2 之後說「數量已更新為 2」），
名稱那一段仍是原值。

## 抬頭的措辭（編劇指定，2026-09-12）

逐字照抄，兩態：

```
正在編輯[實體名稱]名稱，原本數量[數量]保留；如欲更新數量，請點選選單中的「修改數量」選項
```

更新數量之後：

```
正在編輯[實體名稱]名稱，數量已更新為[更新數量]；再度更新數量，請點選選單中的「修改數量」選項
```

它把票券 47 那句（`改的是名稱 —— 數量保留，不必手動重寫`）換掉，理由是**指路**：47 那句只
說了「數量保留」，沒說「那要怎麼改數量」，而這一版把那條路寫在唯一會讀到它的時刻。

⚠️ **所以這兩句要跟 `修改數量…` 那一列同時上線** —— 那一列是這張票造的。在它存在之前先
換文案，等於叫編劇去點一個不存在的選項（票券 47 因此維持原句，2026-09-12 裁決）。

它仍然**不是一列選項**：不進 `rows`、選不到、Enter 碰不到（票券 36 立的分工，47 已實作）。

## 讀不出來時離開 ＝ 第一列的值

空著 Enter、打到一半 Enter、點到外面 —— **都是第一列的值**。修改時那就是原值（或待定值），
不是「若干」。

⚠️ 這一條是票券 41 第一版寫成「若干」、後來被第七輪推翻的地方（使用者裁決 2026-09-12
再確認一次）：**寫成「若干」會讓實作在修改路徑上抹掉原值**，那正是前面好幾輪在補的坑。
新增路徑上第一列本來就是「若干」，所以那邊的結果不變（票券 49）。

Enter 與點到外面必須同一個結果，因為 `blur` 擋不住 —— 點到外面就是走了。若 Enter 擋住、
blur 不擋，同一個念頭就有兩種結果，而且其中一種看不見。

## 為什麼 blocked by 37

驗收裡那條「⌘Z 把打到一半的字還回來」**不是這張票要發明的行為** —— 票券 37 講的就是同一件事
（那串字從來不在 doc 裡，⌘Z 退得回 doc 卻退不回輸入框），只是它發生在實體欄位。

37 沒先修的話，這裡只有兩條路：要嘛在群演欄自己寫一套 undo 還字的機制（於是同一個 bug 有
兩種修法，而且其中一種只蓋得到人數格），要嘛把那條驗收放掉（於是打到一半的字真的會蒸發，
正是票券 41 第三輪說「真正要保住的只有一件事」的那一件）。所以 37 先做，這裡**沿用**它。

## 驗收

- [x] 編輯選單有 `修改數量…` 那一列，位置排在 `↩︎ 不修改，返回` 之前（它是「還要做別的事」，不是「結束」）
- [x] 子選單第一列是 `↰ 不修改數量（8），回上一步`；`若干`、`1`、自由輸入格在它下面
- [x] `1` 那一列與第一列重複時不印
- [x] 沒有猜出來的數字階梯
- [x] 自由輸入格打得出 `8`、`3~5`、`3-5`、`10+`，全形符號一樣認得
- [x] 那一行提示三態都接上了，而且跟著框裡的字變
- [x] 打到一半（`3~`）時它變成警告，**說得出現在離開會記成什麼**
- [x] 空著 Enter、打到一半 Enter、點到外面 —— 三者結果相同，都是第一列的值（修改時是原值，不是若干）
- [x] ⌘Z 把打到一半的字還回來
- [x] 挑完人數**退回描述那一關**，名稱還能接著改，**游標不再整串反白**（停在字尾）
- [x] 只改人數時第一列印 `✏️ 把「路人（8）」改成「路人（2）」`
- [x] 抬頭兩態逐字照編劇指定的措辭，且**與 `修改數量…` 那一列同時上線**
- [x] `↰` 與 `↩︎` 是**兩列**：前者留著名稱的待定改動，後者把待定人數與名稱改動一起丟
- [x] Esc 等同 `↰`（退一階、字留著）
- [x] 再進子選單時第一列跟的是待定值
- [x] 待定人數在按下確認之前**不寫回 doc**
- [x] 本批次之外的呼叫點一個都沒改，**CI 綠**

## Comments

### 2026-09-12 — 實作

十六條驗收全部做進來了，`pnpm typecheck`／`pnpm lint`／全套測試綠（web 522 passed / 34
skipped、schema 238 passed）。群演欄新增 26 條測試，`undo-history.test.tsx` 加 1 條。

**幾個票裡沒裁決、實作時定的：**

1. **人數格是選單裡的一格，`active` 預設就停在它上面。** ↑↓ 要停得進去（它夾在 `1` 與
   `↩︎` 中間，兩側都還有列），而預設停在格子上是驗收逼出來的：打完 `3~5` 按 Enter 要的是
   那串字，不是第一列。停在格子上的 Enter ＝ `commitCount`（讀得出來用它、讀不出來用第一列
   的值），所以「空著 Enter」與「打到一半 Enter」自然與「點到外面」同一個結果 —— 三者走的
   是同一支。

2. ⚠️ **`修改數量…` 上線之後，空框上的 Enter 從「放回」變成「進人數子選單」。** 票券 47
   驗收追加那條寫的是「Enter 打在**看得見的那一列**上」，而框裡空著時看得見的列從一列變成
   兩列，`修改數量…` 排在 `↩︎` 之前（這張票指定的順序），於是它接走了預設那一格。規則一步
   都沒退（Enter 永遠打在選單上停著的那一列），變的是那一列是誰。↓ 一格再 Enter 仍然是放回，
   blur 仍然是放手 —— 兩條都有測試。**這一條請在人工驗收時看一眼**：要換成「空框時預設停在
   `↩︎`」的話，那是把「第一列＝預設」這條通則開一個洞，值不值得由編劇決定。

3. **抬頭的兩句逐字照抄，佔位符照原樣代入**：`[實體名稱]` 用「」框起來（同檔案其餘各處
   引名字的寫法），`[數量]` 直接代 `formatCount` 的結果、前後不補空白 —— 樣板裡沒有空白。
   人數那一段跟待定值走，名稱那一段是原值。空框那一刻的抬頭**維持票券 47 的措辭**（那時它
   說的是下一顆 Backspace 會做什麼，與人數無關）。

4. **人數子選單自己的抬頭**是 `✏️ 修改數量（原本是 8）`（版面圖那一行）。它跟的是**原值**，
   第一列跟待定值 —— 兩者一起看才讀得出「我把它從 8 改成了 2」。

**票券 36 那條分工對過了，結論寫進註解**（`extras-field.tsx` 的人數格那一段）：36 管的是
**選項列**（標籤只印按下去的結果，解釋另外放一行唯讀的）。人數格與它底下那一行**都不是
選項列** —— 格子是輸入、提示是唯讀（`role="presentation"`、選不到、Enter 碰不到）。所以這
一格套的是 36 的另一半：說明歸說明，一行字不兼職當選項。沒有衝突。

**⌘Z 沿用票券 37，這裡一行都沒寫。** `forwardHistoryKey`（掛在 chip row 上）看的是「框裡有
沒有編劇還沒定案的字」，人數格裡的 `3~` 正是那種字，於是那一下歸原生 undo。測試量的是**文件
一步都沒退**（jsdom 沒有原生 undo 堆疊，量不到字本身回來；到得了文件的話 `extras` 會變回一筆，
編輯狀態當場對不上，所以這一條測得出裂縫）。**字真的回不回得來要人工驗收那一下。**

**連帶改到兩個地方（都在這一批的範圍內）：**

- `packages/schema`：`formatCount`／`resolveCountInput`／`countHintText`／`SOME_LABEL`／
  `legacyCount` 開放匯出（票券 44 留的那句「第一個呼叫端出現時再開」兌現），另外多一支
  `extraCount(extra)` —— 畫面需要那個**值**而不只是它印出來的樣子，而「舊資料怎麼讀」
  只能有一個答案（`formatExtra` 改走同一支）。
- `readText` 現在**兩個形態一起寫**（`count` 由 `legacyCount` 算）。票券 47 那一版把
  `countValue` 原封抄回去，舊資料只有 `count` 時它是 `undefined` —— 遷移窗口裡那正是
  票券 44 的 ⚠️ 說「不可以只寫一邊」的情況。兩條既有測試的期望值跟著補上 `countValue`。

### 2026-09-12 — code review 之後

五條 findings，三條修掉、一條切票、一條維持原樣並把理由寫進註解。

**修掉的三條：**

1. **點那一行提示會把整輪編輯定案。** `leaveCountBox` 靠 `relatedTarget` 判斷「人還在這一欄
   裡嗎」，而點到**不可聚焦**的東西（那一行提示、那一列的內距）時瀏覽器給的是 `null` ——
   於是被讀成「走了」。提示就貼在框底下一格，點它多半只是想讀清楚。改成那一列的
   `onMouseDown` 擋掉（格子本身不擋，擋了游標點不進去），與選項列本來的做法同一套。
2. **在子選單裡按 `↩︎` 之後焦點掉到 `body`。** 那一下焦點正停在**即將被卸掉**的格子上，而
   節點被移除時瀏覽器不發 `blur`。`putBack` 現在把焦點送回名稱框（名稱那一關按它時焦點
   本來就在那裡，那一句是無害的重申）。
3. **`extraCount` 的文件說謊。** 它說自己是「舊資料怎麼讀」的唯一答案，但 `bothShapes`
   在「兩個形態矛盾」那一格上裁得相反（舊欄位贏）。**沒有改行為** —— `formatExtra` 從票券
   44 第一天就是 `countValue ?? legacy`，改掉它會動到場次表那一格，不在這一批的範圍。
   改的是文件：把前提（餵進來的每一筆都經過 `sceneExtras`，正規化過就不可能矛盾）與這個
   已知分岔寫清楚，並補一條測試把兩邊的答案同時釘住。

**切成票券 54 的一條：握著一批、框裡空著時按 ⌘Z，同一批人變成兩份。** 當場用探測腳本量過，
**這不是 48 的迴歸** —— 名稱框清空之後按 ⌘Z 一模一樣會發生（票券 47 之後就在）。48 只是讓它
更容易撞到：人數格預設是空的，所以「點 chip → `修改數量…` → ⌘Z」中間不必刪任何字。根因在
`history-keys.ts` 的 `typingInField`（它只問「框裡有沒有沒定案的字」，答不出「這一欄手上握著
東西嗎」），橫跨兩個入口 —— 在這裡補一套擋法正是這張票自己警告的「同一個 bug 兩種修法」。

**維持原樣的一條：滑鼠滑過某一列會把 Enter 改指到它。** 焦點在人數格、Enter 打在停著的那一列
上 —— 這與名稱那一關**逐字同一套行為**（那裡滑過某一列也會改變 Enter 的去向），`is-active`
的反白把它說出來了。在人數這一層另立一條規則，會讓同一份選單有兩種 Enter。

改完之後：`pnpm typecheck`／`pnpm lint` 綠，web 525 passed / 34 skipped、schema 241 passed。
（新增三條測試：點提示不算走了、子選單裡的 `↩︎` 焦點有主人、`extraCount` 的分岔。）

### 2026-09-12 — 人工驗收第 1 條：人數格的 ⌘Z 收在框裡

**回報：** 在人數格裡打字、連按 ⌘Z 直到字退光，**再按一下**欄位裡冒出一顆 `路人（8）`
chip 疊在編輯中的那顆後面（而那顆新的其實就是手上正在編輯的這一批），再一下它又消失。

**裁決（使用者）：** 「那個欄位的 ⌘Z 感覺應該只會影響那個欄位。所以當欄位是空的、沒有之前
的欄位歷史，繼續按 ⌘Z 不會有作用，持續聚焦在那個空欄位而已。」

**做法：** `countKeyDown` 認出歷史鍵就 `stopPropagation()` 收住，**不 `preventDefault`** ——
框裡有字時那一下仍然是原生 undo（票券 37 那條線一步都沒退），框空了就到底。擋的是它往上冒
到 chip row 的 `forwardHistoryKey` 與 window 的 `strayHistoryKey`：那兩支只問「框裡有沒有沒
定案的字」，答不出「這一欄手上握著東西嗎」，於是把「剛拿起這一批」從文件退回來。

`undo-history.test.tsx` 加一條（拿掉守衛會紅：`extras` 從 0 變 1）。web 526 passed。

⚠️ 這一下只關掉**人數格**那個入口。名稱框清空之後的同一顆 ⌘Z 仍然走得到，留在票券 54。

### 2026-09-12 — 人工驗收通過，第二輪措辭與版面調整

十七條全部驗收通過。之後編劇提的調整，**通則是一句話：選項只印結果，解說留給日後的側欄。**

**措辭（逐字指定）：**

| 哪一行 | 原本 | 現在 |
|---|---|---|
| 抬頭・還沒改數量 | `正在編輯「路人」名稱，原本數量8保留；如欲更新數量，請點選選單中的「修改數量」選項` | `正在編輯「路人」群演，原本數量「8」保留` |
| 抬頭・改過數量 | `正在編輯「路人」名稱，數量已更新為2；再度更新數量，請點選…` | `正在編輯「路人」群演，數量已更新：2` |
| 定案那一列 | `✏️ 把「路人（8）」改成「保全（8）」` | `✅ 確認：改成「保全（8）」` |
| 另外開一批 | `＋ 新增「保全（8）」群演，保留「路人（8）」` | `＋ 另外新增「保全（8）」群演` |

抬頭那兩句原本各自把「怎麼改數量」指了一次路 —— 那是**解說**，而它每一次握著群演都會出現。
定案列與另開列各自把「原本是什麼」再唸一遍，可是原值就印在抬頭那一行。兩處都收成結果。
⚠️ 這一輪推翻的是本票上一輪自己定的措辭（2026-09-12 上午那版），不是票券 47。

**記號分家：** `✏️` 原本同時是「還要繼續編輯」（`修改數量…`）與「這一下就結束」（定案列），
又同時是唯讀抬頭的記號 —— 三種語氣一個符號。現在 `✏️` 只留給**還要繼續**的動作，新增
`✅ CONFIRM_MARK`（定案）與 `💡 HINT_MARK`（唯讀說明，跟著票券 36 那條分工走）。
**人物／地點欄的抬頭一起換成 `💡`** —— 兩欄並排，不該一邊 `💡` 一邊 `✏️`。
（換成一套 SVG 圖庫是另一件事，開成**票券 55**。）

**版面：自由輸入格上移到第二列**（緊跟著 `↰`）。原本排在 `若干`／`1` 底下，按 `修改數量…`
的那一瞬間焦點跳到選單第四列，跨過半份選單、看不見它跳去哪。
另一條路「先把焦點給第一列」**沒有採用**：打完 `3~5` 按 Enter 會需要多點一次框，而
「空著 Enter／打到一半 Enter／點到外面三者同結果」正是靠格子預設被聚焦才成立的。

**輸入格的樣式：** 原本透明無底色，讀起來像「這一列空缺了」。給了底色、聚焦時吃同一條
`--focus-ring`，並加上 placeholder `輸入人數` —— 格式舉例仍然只在底下那一行提示裡說
（票券 36：一行字不兼職）。

測試跟著改：既有斷言換措辭，另加兩條（格子排第二列、placeholder）。
`pnpm typecheck`／`pnpm lint` 綠，web 528 passed / 34 skipped、schema 241 passed。
